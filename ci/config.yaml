checks:
  required_workflows:
    codeql.yaml: False
    audit.yaml: False
    dependabot-auto-merge.yaml: False
  versions: False
  dependabot_config: False

publish:
  pypi:
    packages: []
  docker:
    images:
      - name: camptocamp/mapserver
