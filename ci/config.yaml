# yaml-language-server: $schema=https://raw.githubusercontent.com/camptocamp/c2cciutils/1.0/c2cciutils/schema.json

checks:
  required_workflows: false
  versions: false
  black: false

publish:
  pypi:
    packages: []
  docker:
    images:
      - name: camptocamp/mapserver
